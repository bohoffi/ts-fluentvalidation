import {NgDocPage} from '@ng-doc/core';
import OtherFeatures from '../ng-doc.category';

const CustomState: NgDocPage = {
	title: `Custom State`,
	mdFile: './index.md',
	route: `custom-state`,
	category: OtherFeatures,
	order: 9,
};

export default CustomState;
